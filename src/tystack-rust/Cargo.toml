[package]
name = "tystack-rust"
version = "0.1.0"
authors = ["Brian Anderson <banderson@mozilla.com>"]

[lib]
path = "lib.rs"

[dependencies]

tystack-core = { path = "../tystack-core" }
tystack-rt = { path = "../tystack-rt" }
